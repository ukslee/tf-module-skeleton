---
driver:
  name: terraform
  root_module_directory: test/fixtures

provisioner:
  name: terraform

verifier:
  name: terraform

platforms:
  - name: default
    drivier:
      root_module_directory: test/fixtures/default

    verifier:
      systems:
        - name: local
          backend: local
          controls:
            - state_file
            - terraform_output

suites:
  - name: default
